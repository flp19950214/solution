创新性：

第一题： 并发+分组+去重+并发
并发：顺序读取文件后，以行为单位放到线程池中做逻辑处理
分组：以“合约号+分钟” 分组处理
去重：相同合约 使用TreeSet去重并按时间排序
并发2：分组后，再次异步处理不同合约的最大成交量。
(本题是计算一分钟内数据，考虑到同一合约，在同一分钟的数据量不会多。所以无需特殊处理，单层循环比较即可)

第二题： 并发+分组+并发+并发+去重+排序+分段
并发：顺序读取文件后，以行为单位放到线程池中做逻辑处理
分组：以“合约号” 分组处理
并发2：分组后，再次异步处理不同合约的最大收益。
并发3+去重+排序+分段：当某个合约的数量过多时，对集合数据按下标分段处理，找出同一合约中两条数据差值最大的。
           举例说明：如果某个合约交易数据是20000条。并发是2,时间复杂度则由O(n^2)降为O(n^2/4)
