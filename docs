第一题： 并发+分治+去重+并发
并发：顺序读取文件后，以行为单位放到线程池中做逻辑处理
分治：以“合约号+分钟” 分组处理
去重：相同合约 使用TreeSet去重并按时间排序
并发2：分组后，再次异步处理不同合约的最大成交量。


第二题： 并发+分治+并发+并发+分段
并发：顺序读取文件后，以行为单位放到线程池中做逻辑处理
分治：以“合约号” 分组处理
并发2：分组后，再次异步处理不同合约的最大收益。
并发3+分段：当某个合约的数量过多时，对集合数据按下标分段处理，找出同一合约中两条数据差值最大的。